# ตัวอย่างค่า ENV สำหรับ backend
# ใช้ร่วมกับ docker-compose.yml ที่ root ของโปรเจกต์

# Database connection string สำหรับ Postgres (ผ่าน Docker)
DATABASE_URL="postgresql://postgres:postgres@db:5432/regdog?schema=public"

# ถ้ารันนอก Docker (macOS) อาจเป็น
# DATABASE_URL="postgresql://postgres:postgres@localhost:5432/regdog?schema=public"
